# Library versions used during development

- ArduinoJson: 6.x or 7.x
- TFT_eSPI: recent GC9A01-compatible release
- WiFiManager: recent tzapu/WiFiManager release
- TinyGPSPlus: only required if `ENABLE_GPS` is set to 1
